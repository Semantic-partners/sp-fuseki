# Build artifacts
/dist/
/build/
*.tar.gz

# Effective config produced by the entrypoint at runtime (names must match
# entrypoint.clj — it writes these under FUSEKI_BASE)
/run/
config.effective.ttl
shiro.ini

# Local env
.env
*.local

# OS / editor
.DS_Store

# Clojure tooling caches (clj-kondo/clojure-lsp, created by editors and bb)
/.clj-kondo/
/.lsp/
